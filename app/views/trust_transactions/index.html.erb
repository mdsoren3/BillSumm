<h1>Listing trust_transactions</h1>

<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Transactionamount</th>
      <th>Transactiontype</th>
      <th>Note</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @trust_transactions.each do |trust_transaction| %>
      <tr>
        <td><%= trust_transaction.date %></td>
        <td><%= trust_transaction.transactionAmount %></td>
        <td><%= trust_transaction.transactionType %></td>
        <td><%= trust_transaction.note %></td>
        <td><%= link_to 'Show', trust_transaction %></td>
        <td><%= link_to 'Edit', edit_trust_transaction_path(trust_transaction) %></td>
        <td><%= link_to 'Destroy', trust_transaction, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Trust transaction', new_trust_transaction_path %>
